// v12.80 — herschrijf exchange-lessons (l18-l22) in een compactere vorm.
// Behoudt alle kritische info (90-dagen-trap, EU-regelgeving, privacy, US-vs-Global)
// maar schrapt dubbele uitleg, lange glossary-blokken en niet-essentiële troubleshooting.
//
// Aanpak: vervang per lesson de volledige content-string. Behoudt id/level/svg/title/desc.
const fs = require('fs');
const path = require('path');

const FILE = path.resolve(__dirname, '../work/tradejournal.html');
let html = fs.readFileSync(FILE, 'utf8');

const TABS_TEMPLATE = (currentId) => {
  const exchanges = [
    { id: 'l18', emoji: '🟣', name: 'Blofin' },
    { id: 'l19', emoji: '🟢', name: 'MEXC' },
    { id: 'l20', emoji: '🟠', name: 'Kraken Futures' },
    { id: 'l21', emoji: '🔵', name: 'Hyperliquid' },
    { id: 'l22', emoji: '🔴', name: 'FTMO (MT5)' },
  ];
  const tabs = exchanges.map(e =>
    e.id === currentId
      ? `<button class="active" aria-current="page">${e.emoji} ${e.name}</button>`
      : `<button class="lesson-link" data-lesson-target="${e.id}">${e.emoji} ${e.name} →</button>`
  ).join('');
  return `<div class="lesson-exchange-tabs">${tabs}</div>`;
};

const FOOTER = (date) => `<button class="lesson-link" data-target="accounts">Open Accounts →</button><p style="margin-top:18px;font-size:11px;color:var(--text5);font-style:italic">Laatst gecontroleerd: ${date} · UI's veranderen — meld in #syncjournal-feedback als deze stappen niet meer kloppen.</p>`;

// === BLOFIN (l18) ===
const L18 = TABS_TEMPLATE('l18') + `<div class="lesson-tldr"><div class="lesson-tldr-label">TL;DR · 30 seconden</div><ul><li>CSV: Assets → Order Center → <strong>Order History</strong> → Download. Maximum 180 dagen per export.</li><li>API: profielicoon → API Management → <strong>alleen Read</strong>. Vereist passphrase (zelf gekozen).</li><li>Zonder IP-whitelist verloopt key na <strong>90 dagen</strong> — sync stopt zonder waarschuwing.</li><li>Mobiele app exporteert <strong>niet</strong> — alleen via desktop-browser.</li></ul></div><p>Twee manieren om Blofin-trades in SyncJournal te krijgen: CSV-export (eenmalig) of API-koppeling (live sync).</p><h2>Pad A — CSV exporteren</h2><ol><li>Login op <strong>blofin.com</strong> via desktop-browser.</li><li>Klik <strong>Assets → Order Center → Order History</strong>.</li><li>Kies <strong>Futures</strong> + datumbereik (max 180 dagen) → <strong>Download</strong>.</li><li>SyncJournal → <strong>Instellingen → Accounts → Blofin</strong> → <strong>📄 Importeer CSV / XLSX</strong> → bevestig preview.</li></ol><div class="lesson-callout warn"><div class="lc-title">Meer dan 180 dagen of funding fees?</div>Stuur een ticket naar <code>support@blofin.com</code> met UID + tijdsbereik + tekst "CSV". Funding fees zitten niet in de standaard-export.</div><h2>Pad B — API-koppeling</h2><ol><li>blofin.com → <strong>profielicoon rechtsboven → API → Create API Key</strong>.</li><li>Naam: <code>SyncJournal</code>. Permissions: <strong>alleen Read</strong>. Trade en Withdraw uit.</li><li>Kies een <strong>passphrase</strong> en <strong>schrijf 'm op</strong> — Blofin toont 'm later niet meer.</li><li>2FA + e-mail-verificatie. <strong>API Secret wordt eenmalig getoond</strong> — kopieer direct.</li><li>SyncJournal → Accounts → Blofin → plak <strong>API Key + Secret + Passphrase</strong> → <strong>Verbinding testen</strong>.</li></ol><div class="lesson-callout warn"><div class="lc-title">⏰ De 90-dagen-trap</div>Een API-key zonder IP-whitelist verloopt na <strong>90 dagen</strong>. Twee opties: (1) statisch IP whitelisten = onbeperkt geldig, of (2) zonder whitelist = elke 90 dagen nieuwe key (kalender-reminder).</div><div class="lesson-callout why"><div class="lc-title">Waarom alleen "Read"?</div>Lekt de key (browser-extension, malware), dan kan een aanvaller hooguit kijken — niet trades plaatsen of geld opnemen.</div><h2>Pitfalls</h2><ul><li><strong>Mobile-app exporteert niet</strong> — gebruik desktop.</li><li><strong>Contract size BTC = 0.001/contract</strong> — SyncJournal converteert automatisch.</li><li><strong>Partial closes</strong>: meerdere CSV-rijen per positie; SyncJournal aggregeert via FIFO.</li><li><strong>Passphrase vergeten</strong> = key onbruikbaar; nieuwe maken.</li></ul><h2>Trades komen niet binnen?</h2><ol><li><strong>Verbinding faalt</strong>? Check Key/Secret/Passphrase op exact-copy (geen spaties).</li><li><strong>Preview leeg</strong>? CSV moet headers <code>Underlying Asset</code>, <code>Avg Fill</code>, <code>PNL</code> hebben — anders verkeerd export-type.</li><li><strong>Sync stopt</strong>? Check 90-dagen-window — maak nieuwe key.</li></ol>` + FOOTER('2026-05-02');

// === MEXC (l19) ===
const L19 = TABS_TEMPLATE('l19') + `<div class="lesson-tldr"><div class="lesson-tldr-label">TL;DR · 30 seconden</div><ul><li>CSV: Orders → Futures Orders → <strong>Position History</strong> → Export. Maximum 18 maanden.</li><li>API: profielicoon → API Management → <strong>alleen Read</strong>. <strong>Geen passphrase</strong> nodig.</li><li>Zonder IP-whitelist verloopt key na 90 dagen — zelfde trap als Blofin.</li><li>Mobiele app kan <strong>niet</strong> exporteren.</li></ul></div><p>MEXC is populair voor crypto-futures met hoge altcoin-variëteit. CSV-export of API-koppeling.</p><h2>Pad A — CSV exporteren</h2><ol><li>Login op <strong>mexc.com</strong> via desktop-browser.</li><li>Rechtsboven → <strong>Orders → Futures Orders → Position History</strong>.</li><li>Datumbereik (max 18 maanden) → <strong>Export Position History</strong> → kies <strong>CSV</strong> of Excel (geen PDF — niet parsable).</li><li>SyncJournal → Accounts → MEXC → <strong>📄 Importeer CSV / XLSX</strong> → bevestig.</li></ol><div class="lesson-callout warn"><div class="lc-title">Spot ≠ Futures</div>Position History is alleen voor futures-trades. Spot-history zit elders en wordt niet herkend.</div><h2>Pad B — API-koppeling</h2><ol><li>mexc.com → <strong>profielicoon → API Management</strong>. KYC moet voltooid zijn.</li><li>Klik <strong>Create API</strong>. Naam: <code>SyncJournal</code>. Permissions: <strong>alleen Read</strong>.</li><li>2FA-bevestiging. Secret Key eenmalig getoond — kopieer direct.</li><li>SyncJournal → Accounts → MEXC → plak <strong>API Key + Secret</strong> (geen passphrase!) → <strong>Verbinding testen</strong>.</li></ol><div class="lesson-callout warn"><div class="lc-title">⏰ 90-dagen-trap</div>Zonder IP-whitelist verloopt je key na 90 dagen — sync stopt stilletjes. Whitelist je IP voor onbeperkte geldigheid.</div><h2>Pitfalls</h2><ul><li><strong>Geen passphrase-veld</strong> bij MEXC — alleen Key + Secret. Zie je toch een passphrase-veld? Verkeerde exchange-pagina.</li><li><strong>Position History bevat geen partial closes apart</strong> — alleen volledige cycli. Liquidaties wel.</li><li><strong>Max 100k entries / 10 downloads per maand</strong> — power-user detail.</li><li><strong>Mobile-app exporteert niet</strong>.</li></ul><h2>Trades komen niet binnen?</h2><ol><li><strong>Test fails</strong>? Check Key/Secret op exact-copy (geen spaties).</li><li><strong>Preview leeg</strong>? Headers <code>Avg Entry Price</code> + <code>Avg Close Price</code> + <code>Closing PNL</code> aanwezig? Anders is 't Order History — kies Position History.</li><li><strong>Sync stopt</strong>? 90-dagen-window verlopen? Maak nieuwe key.</li></ol>` + FOOTER('2026-05-03');

// === KRAKEN FUTURES (l20) ===
const L20 = TABS_TEMPLATE('l20') + `<div class="lesson-tldr"><div class="lesson-tldr-label">TL;DR · 30 seconden</div><ul><li><strong>futures.kraken.com</strong> is een aparte site — niet www.kraken.com. Spot-keys werken niet.</li><li>CSV: <strong>Logs → Download All</strong>. Account-log met trades + funding rates.</li><li>API: Settings → API → <strong>Read Only</strong>. Geen passphrase.</li><li>⚠ EU-retail 2026: 10× leverage, ESMA dreigt H2 2026 met 2× cap.</li></ul></div><p>Kraken Futures heeft een aparte interface en aparte API-keys, los van Kraken-spot.</p><div class="lesson-callout warn"><div class="lc-title">EU-traders: regelgeving in beweging</div>Sinds nov 2025 EU-retail mogelijk via CySEC-entiteit (max 10× hefboom). ESMA publiceerde feb 2026 dat perpetuals waarschijnlijk onder CFD-product-intervention vallen → leverage zou H2 2026 naar 2× kunnen zakken. Mei 2026 nog niet doorgevoerd.</div><h2>Pad A — CSV exporteren</h2><ol><li>Login op <strong>futures.kraken.com</strong> (let op: <em>futures.</em>kraken.com).</li><li>Linkermenu → <strong>Logs</strong>.</li><li>Onderaan: <strong>"Download All"</strong> → CSV downloadt direct.</li><li>SyncJournal → Accounts → Kraken → <strong>📄 Importeer CSV / XLSX</strong>.</li></ol><h2>Pad B — API-koppeling</h2><ol><li>futures.kraken.com → naam rechtsboven → <strong>Settings → tab API → "Create Key"</strong>.</li><li>Naam: <code>SyncJournal</code>. Permission: <strong>"Read Only"</strong>. Withdrawal + Transfer UIT.</li><li>Optioneel: IP whitelist (kan "no expiration" of custom periode).</li><li>SyncJournal → Accounts → Kraken → plak <strong>API Key + Secret</strong> (geen passphrase!) → <strong>Verbinding testen</strong>.</li></ol><div class="lesson-callout warn"><div class="lc-title">Spot ≠ Futures keys</div>Een Kraken-spot API-key werkt niet voor futures (andere base-URL, andere auth). Gebruik altijd een sleutel uit futures.kraken.com.</div><h2>Pitfalls</h2><ul><li><strong>Funding rates zijn aparte rijen</strong> in Account Log — geen trades. SyncJournal toont ze als kostenpost.</li><li><strong>Partial closes</strong>: elke fill = aparte rij; aggregatie via contract + tijdvolgorde.</li><li><strong>Geen passphrase</strong> — anders dan Blofin/OKX.</li></ul><h2>Trades komen niet binnen?</h2><ol><li><strong>Test fails</strong>? Check je op futures.kraken.com inlogt, niet www.kraken.com.</li><li><strong>Preview leeg</strong>? Bevat de CSV <code>booking_uid</code> + <code>trade_price</code> + <code>realized_pnl</code>? Anders verkeerd export-type.</li><li><strong>Geen funding zichtbaar</strong>? Account Log bevat ze; SyncJournal toont als context, niet als trade. Geen bug.</li></ol>` + FOOTER('2026-05-03');

// === HYPERLIQUID (l21) ===
const L21 = TABS_TEMPLATE('l21') + `<div class="lesson-tldr"><div class="lesson-tldr-label">TL;DR · 30 seconden</div><ul><li>Geen API-key — alleen je <strong>0x-wallet-adres</strong> (42 chars).</li><li>⚠ <strong>Privacy</strong>: alle Hyperliquid-trades zijn <strong>publiek</strong> on-chain (leaderboard, HyperTracker, Coinglass).</li><li>CSV: Portfolio → Trade History → Export. Hard limit <strong>10.000 rijen</strong>.</li><li>Sub-accounts en vaults hebben eigen adres.</li></ul></div><p>Hyperliquid is een fully-onchain perp-DEX. Geen API-key + secret — alleen je public wallet-adres.</p><div class="lesson-callout warn"><div class="lc-title">⚠ Privacy: alle trades zijn publiek</div>Tools als HyperTracker, Coinglass HL en de officiële Hyperliquid Leaderboard tonen elke positie, fill, PnL en win-rate van elk wallet-adres. Door je adres aan SyncJournal te geven deel je niets nieuws — alles staat al openbaar. <strong>Tip</strong>: gebruik een dedicated trading-wallet, niet eentje met persoonlijke ENS-namen of NFT's.</div><h2>Pad A — Wallet-adres koppelen (1 minuut)</h2><ol><li>Open <strong>app.hyperliquid.xyz</strong> en login met je wallet (MetaMask/Rabby/Phantom/Ledger).</li><li>Wallet-adres staat <strong>rechtsboven</strong> → klik om te kopiëren.</li><li>Format: <code>0x</code> + 40 hex chars = 42 totaal.</li><li>SyncJournal → Accounts → Hyperliquid → plak in <strong>Wallet-adres</strong>.</li><li>Klik <strong>Verbinding testen</strong> + <strong>📡 Open posities ophalen</strong>.</li></ol><h2>Pad B — CSV-export (voor &gt;10.000 fills)</h2><ol><li>app.hyperliquid.xyz → <strong>Portfolio → Trade History → "Export as CSV"</strong>.</li><li>Bevat <code>time, coin, dir, px, sz, ntl, fee, closedPnl</code>.</li><li>SyncJournal → Accounts → Hyperliquid → <strong>📄 Importeer CSV / XLSX</strong>.</li></ol><div class="lesson-callout tip"><div class="lc-title">Voor extreem actieve traders</div>Hard limit native CSV: 10.000 rijen. Voor meer: community-tool <code>trade-export.hypedexer.com</code> (Enigma team) — geen limit, exporteert via wallet-adres.</div><h2>Pitfalls</h2><ul><li><strong>Sub-accounts/vaults hebben eigen adres</strong> — gebruik het adres dat <em>daadwerkelijk</em> de trades plaatste, niet altijd je master-EOA.</li><li><strong>Geen native posities</strong> — alles zijn fills. SyncJournal reconstrueert round-trips.</li><li><strong>Open posities</strong> hebben <code>closedPnl=0</code>; tonen als open, niet als verlies.</li><li><strong>Meerdere wallet-extensies</strong>: Rabby/Phantom kan MetaMask intercepten. Tijdelijk uitzetten lost dat op.</li></ul><h2>Trades komen niet binnen?</h2><ol><li><strong>Test fails</strong>? Check of adres exact 42 chars is en met <code>0x</code> begint.</li><li><strong>Geen trades</strong>? Trade je via een vault of sub-account? Gebruik <em>dat</em> adres in plaats van je master.</li><li><strong>Sync stopt plotseling</strong>? Hyperliquid API rate-limit — wacht 5 min en probeer opnieuw.</li></ol>` + FOOTER('2026-05-03');

// === FTMO MT5 (l22) ===
const L22 = TABS_TEMPLATE('l22') + `<div class="lesson-tldr"><div class="lesson-tldr-label">TL;DR · 30 seconden</div><ul><li><strong>Geen API</strong> beschikbaar — altijd via CSV-export uit MetriX.</li><li>Pad: <code>trader.ftmo.com</code> → Accounts → klik account → <strong>MetriX → Trading Journal → Export CSV</strong>.</li><li>Date-range filter <strong>boven</strong> de tabel — eerst filteren, dan exporteren.</li><li>Pitfall: <strong>FTMO US</strong> (netting/FIFO) vs <strong>FTMO Global</strong> (hedging) — radicaal andere CSV-output.</li></ul></div><p>FTMO is een prop-firm voor forex/CFD. Geen API beschikbaar voor third-party journals — koppeling gaat via CSV uit FTMO's eigen MetriX-dashboard.</p><h2>CSV exporteren via MetriX</h2><ol><li>Login op <strong>trader.ftmo.com</strong>.</li><li>Linker zijbalk → <strong>Accounts Overview</strong> → klik op het account.</li><li>Tab <strong>MetriX</strong> → scroll naar <strong>Trading Journal</strong>.</li><li>Boven de tabel: <strong>date-range picker</strong> → kies periode.</li><li>Blauwe <strong>Export</strong>-knop → kies <strong>CSV</strong>.</li><li>SyncJournal → Accounts → FTMO → <strong>📄 Importeer CSV / XLSX</strong>.</li></ol><div class="lesson-callout tip"><div class="lc-title">Parser herkent MetriX automatisch</div>Headers <code>Ticket</code> + <code>Pips</code> + <code>Volume</code> + <code>Swap</code> in eerste rij = goed. Anders is 't een MT5-desktop-export — die wordt nog niet herkend.</div><h2>FTMO US vs FTMO Global — kritisch verschil</h2><ul><li><strong>FTMO Global</strong> (hedging): één ticket per trade, schoon mapping. Partial closes = aparte tickets met dezelfde positionId. SyncJournal groepeert automatisch.</li><li><strong>FTMO US</strong> (netting/FIFO): één <em>net position</em> per symbool met <strong>average price</strong>. Bij partial close geen aparte tickets, alleen volume-reductie. CSV toont één rij met cumulatieve PnL.</li></ul><div class="lesson-callout warn"><div class="lc-title">Bij FTMO US: één rij dekt vaak meerdere intenties</div>Average-entry verbergt je losse fills. Trade die je opende op 1.0850 en bijschaalde op 1.0880 verschijnt als één rij met average 1.0865. Tip: maak vóór elke trade een entry-notitie zodat je 't terug kunt zien.</div><h2>Pitfalls</h2><ul><li><strong>Geen API ooit</strong> — FTMO's eigen partner-API geeft alleen account-stats, geen individuele trades.</li><li><strong>Symbol-format</strong>: <code>EURUSD</code> / <code>BTCUSD</code> / <code>US30</code> / <code>XAUUSD</code> (geen slash, geen suffix).</li><li><strong>Account-types</strong> Challenge/Verification/Funded: identiek CSV-format.</li><li><strong>Trial accounts zonder eerste trade</strong>: MetriX-tab is dan leeg.</li></ul><h2>Trades komen niet binnen?</h2><ol><li><strong>Preview leeg</strong>? Eerste rij <code>Ticket,Pips,Volume,Swap</code>? Anders is 't een MT5-export — gebruik MetriX in plaats.</li><li><strong>Volume oogt vreemd</strong>? FTMO levert lots (1 lot = 100k units bij forex). SyncJournal converteert; bij twijfel meld in feedback.</li><li><strong>Average-entry past niet bij plan</strong>? Je trade op een netting-account (FTMO US). Zie sectie hierboven.</li></ol>` + FOOTER('2026-05-03');

// Vervang per lesson de hele content-string. Match: id:"lXX",...content:`...`},
function replaceLessonContent(html, lessonId, newContent) {
  const startMarker = `id:"${lessonId}",`;
  const startIdx = html.indexOf(startMarker);
  if (startIdx < 0) throw new Error(`${lessonId} not found`);
  const contentMarker = 'content:`';
  const contentStart = html.indexOf(contentMarker, startIdx);
  if (contentStart < 0) throw new Error(`${lessonId} content marker not found`);
  // Vind de afsluitende `}, — backtick gevolgd door comma+newline (of einde lesson)
  // We zoeken het volgende `},\n` na contentStart
  let i = contentStart + contentMarker.length;
  while (i < html.length) {
    // Een backtick gevolgd door }, of }] = einde van de content-string
    if (html[i] === '`' && (html.slice(i, i + 3) === '`},' || html.slice(i, i + 3) === '`}]')) {
      break;
    }
    // Skip backslash-escapes binnen de string
    if (html[i] === '\\' && i + 1 < html.length) { i += 2; continue; }
    i++;
  }
  if (i >= html.length) throw new Error(`${lessonId} closing backtick not found`);
  const before = html.slice(0, contentStart + contentMarker.length);
  const after = html.slice(i); // begint met `
  return before + newContent + after;
}

html = replaceLessonContent(html, 'l18', L18);
console.log('l18 herschreven');
html = replaceLessonContent(html, 'l19', L19);
console.log('l19 herschreven');
html = replaceLessonContent(html, 'l20', L20);
console.log('l20 herschreven');
html = replaceLessonContent(html, 'l21', L21);
console.log('l21 herschreven');
html = replaceLessonContent(html, 'l22', L22);
console.log('l22 herschreven');

fs.writeFileSync(FILE, html, 'utf8');
console.log('\nDone — alle 5 lessons compacter');
