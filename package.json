{
  "name": "tradejournal-tests",
  "version": "1.0.0",
  "description": "Dev-only test harness for SyncJournal. Niet voor distributie — alleen lokaal gebruik door Claude Code agent en developers.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js",
    "test:partial": "playwright test tests/blofin-partial.spec.js",
    "adhoc": "node tests/run-adhoc.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
